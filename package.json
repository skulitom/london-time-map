{
  "name": "london-time-map",
  "version": "1.0.0",
  "private": true,
  "description": "A dark-mode map of London that stretches and shrinks by travel time.",
  "type": "module",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node --test tests/browser/*.test.mjs",
    "serve": "python -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
